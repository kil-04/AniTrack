const { app } = require('electron'); app.whenReady().then(async () => { const pahe = require('./dist-electron/electron/services/animepahe.js'); try { const res = await pahe.search('Iruma'); const s4 = res.find(x => x.title.includes('Season 4')); const eps = await pahe.getEpisodes(s4.session); console.log(JSON.stringify(eps.data, null, 2)); } catch(e) { console.error(e); } app.quit(); });
