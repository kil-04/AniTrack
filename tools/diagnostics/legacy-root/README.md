# Archived provider diagnostics

These files are historical standalone probes and captured pages that previously
lived in the repository root. They are preserved for provider debugging but are
not production source, automated tests, build inputs or release scripts.

New repeatable checks belong in `tests/`. Short-lived investigations belong in
`scratch/` and should become a fixture test when they capture a real bug.
